# anonshare web app environment variables
# Copy to .env and fill in values. Never commit .env to source control.

# Public base URL used to construct share links (no trailing slash)
APP_BASE_URL=http://localhost:3000

# Internal API URL the web server calls for server-side requests
APP_API_URL=http://localhost:3001
